@model RedirectUriViewModel

<div class="row">
    <div class="col-md-8">
        <form asp-action="AddRedirectUri" asp-route-id="@Model.Id" class="form-inline"
              asp-authorize="@AuthorizationPolicies.WholeApplicationAdministrationPolicy">
            <div class="form-group">
                <label asp-for="Value" class="control-label">Add redirect URI:</label>
                <input asp-for="Value" class="form-control" />
            </div>
            <div class="form-group">
                <input type="submit" value="Add" class="btn btn-primary" />
                <span asp-validation-for="Value" class="text-danger"></span>
            </div>
        </form>
    </div>
</div>
