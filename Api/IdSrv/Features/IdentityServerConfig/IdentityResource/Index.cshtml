@model IEnumerable<IdentityResourceSummaryViewModel>
@{
    ViewData["Title"] = "Identity Resources";
    Layout = "_Layout";
}

<h2>Identity Resources</h2>

<table class="table table-striped">
    <thead>
        <tr>
            <th>@Html.DisplayNameFor(m => m.DisplayName)</th>
            <th>@Html.DisplayNameFor(m => m.Name)</th>
            <th>@Html.DisplayNameFor(m => m.Id)</th>
            <th class="text-center">Actions</th>
        </tr>
    </thead>
    <tbody>
        @foreach (var identityResource in Model)
        {
            <tr>
                <td>@identityResource.DisplayName</td>
                <td>@identityResource.Name</td>
                <td>@identityResource.Id</td>
                <td class="text-center">
                    <form asp-action="Delete" asp-route-id="@identityResource.Id">
                        
                        <a 
                           asp-action="Edit" 
                           asp-route-id="@identityResource.Id" 
                           class="btn btn-sm btn-warning">edit</a>

                        <button type="submit" class="btn btn-sm btn-danger">delete</button>
                    </form>

                </td>
            </tr>
        }
    </tbody>
</table>

<a asp-action="Add" class="btn btn-primary">New Identity Resource</a>
