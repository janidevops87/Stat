{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "System": "Information",
      "Microsoft": "Information"
    },
    "Email": {
      "EmailSender": {
        // To be able to filter out messages from the
        // test environment.
        "EmailSubject": "[Test] Copernicus API Logger"
      },
      "LogLevel": {
        "Default": "Error",
        // Email logs about critical events like unhandled exceptions.
        "Statline": "Critical"
      }
    }
  },

  "Authentication": {
    "IdentityServerUri": "https://testid.statline.org/"
  },

  "Azure": {
    "KeyVault": {
      "VaultName": "https://kv-stattrac-test.vault.azure.net/"
    }
  },

  "StatTracApiClient": {
    "ServerUrl": "https://app-stapi-test.azurewebsites.net/",
    "Authentication": {
      "Address": "https://testid.statline.org/connect/token"
    }
  }
}
